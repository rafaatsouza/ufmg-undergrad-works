#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "Funcoes/Funcoes.h"

int main(){
    srand((unsigned)time(NULL));
    // vetor *v = criaVetor(1,10,10);
    // imprimeVetor(v);
    // defineOrdenado(v);
    // ordenaBolha(v);
    // imprimeVetor(v);
    // defineOrdenado(v);
    return 0;
}
